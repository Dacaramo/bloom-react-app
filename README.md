# Como probar bloom-react-app
* Ubicarse en la carpeta que contiene el folder src y el archivo package.json
* Correr el comando `npm start`
> La aplicación de React correrá por defecto en el puerto 3000.
